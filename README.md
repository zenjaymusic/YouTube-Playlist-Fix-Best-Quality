# YouTube-Playlist-Fix-Best-Quality
Fixes two Opera GX YouTube bugs:  
Playlist clicks — Opera GX uses YouTube's AJAX navigation, which breaks playlist links, so the script intercepts every video link click and forces a real page load instead.
Video quality — automatically selects the highest available quality on every video, since YouTube defaults to lower quality on load
